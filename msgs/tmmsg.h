#define tmFTL001 5501 /* Too many transactions. */
#define tmFTL002 5502 /* tmstrt: transaction already active. */
#define tmFTL004 5503 /* tmrej called, but no transaction active. */
#define tmFTL005 5504 /* tmstrt: Cannot start a transaction while rready-to */
#define tmFTL006 5505 /* Tried to end a LIMBO transaction. */
#define tmFTL007 5506 /* tmdlg: Negative # of rcomm notes in the table. */
#define tmMSG001 5507 /* Trid: %l %s. */
#define tmMSG002 5508 /* Transaction %l, on coordinator %s #%l, is in a lim */
#define tmMSG003 5509 /* Commit transaction %l, on coordinator %s #%l (y co */
#define tmMSG004 5510 /* Are you sure you want to commit %l (y/n)? */
#define tmMSG005 5511 /* Are you sure you want to abort %l (y/n)? */
#define tmMSG006 5512 /* See list on the *.lg file. Use proutil %s -C 2phas */
#define tmMSG007 5513 /* The database contains limbo transactions. */
#define tmMSG008 5514 /* tmstrt: free slot %i has transnum %l */
#define tmMSG009 5515 /* User Id: %s. */
